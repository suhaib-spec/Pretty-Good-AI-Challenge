# Pretty-Good-AI-Engineering-Challenge
I built it

to run execute in terminal: 

python -m venv .venv
.venv\Scripts\activate    
python.exe -m pip install --upgrade pip
pip install -r requirements.txt
python app.py


after backend server is running run in a separate terminal: 
ngrok http 5000

link is: unprating-distractively-luetta.ngrok-free.ap
Update it in retell.ai
